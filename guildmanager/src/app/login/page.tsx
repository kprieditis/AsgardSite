import { PlaceholderPanel } from "@/components/asgard/placeholder-panel"

export default function LoginPage() {
  return (
    <PlaceholderPanel
      eyebrow="Authentication"
      title="Login"
      description="Discord OAuth login will be added in Phase 2. For now, this page reserves the login route."
    />
  )
}